# Hybric Example Mod

A quick and easy example mod for Hybric. 

Build it with `./gradlew build` (or `./gradlew :mod:build` for the subproject), then place the built jar (in `build/libs`) into the mods folder.